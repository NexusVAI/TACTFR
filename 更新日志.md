# 更新日志

## 5.3.99 (2026-02-16)

- 屏蔽双人案件入口：终端仅保留单人案件（步行嫌疑人/驾车逃逸）与巡逻模式入口；保留双人代码结构与资源，后续可通过 `FeatureGates.EnableDualSuspectCase` 快速恢复。
- 屏蔽 F7 车队入口：输入层与调度系统都增加功能开关保护；当前版本默认不可访问车队调度。
- 车队系统保留标准化接口：`DispatchSupportSystem` 的 `RequestBackup/SetConvoyFollowPlayer/SetConvoyFreeRoam` 与 Tick 路径均按开关受控，便于后续重构后重新启用。
- 双人案件相关运行时生成逻辑改为默认单人执行（即使收到旧版双人 optionId 也自动回退单人）。
- 更新加载提示文案为：
  `5.3.99-TACTFR警察模组加载成功！玩家动力@某宇原创制作 最后更新时间2026/02/16，祝大家新年快乐~模组QQ反馈群1079691553`
